# Frontend Daily Note — 2026-08-14

Written by Claude. Started the day still under the "Copilot can't create
PRs" workaround (lost GitHub API token, git push still works). Partway
through, Copilot ran out of tokens entirely — a harder blocker, it can't
do any work at all — so everything from that point on is Claude coding
`frontend/` directly, as a documented exception, not just opening PRs for
Copilot's commits.

## Done today

- **Copilot delivered issue #44** (login/register UI, JWT interceptor,
  route guard, logout) across two review rounds before merging as
  [PR #46](https://github.com/nahunp/todo-app/pull/46):
  - Round 1: request/response field names didn't match the backend's
    actual contract (`username`/`token` vs. `email`/`accessToken`), and
    the interceptor was registered the classic `HTTP_INTERCEPTORS` way
    with no `withInterceptorsFromDi()`, so it silently never ran. Sent
    back with specifics, no PR opened.
  - Round 2: all three fixed, plus the guard/logout/functional-
    interceptor work — genuinely solid, but didn't compile (two
    one-line type/import errors) and a test regression CI caught that
    local `ng build` alone didn't (`ActivatedRoute` provider missing
    after `RouterLink` was added to the nav). Small enough to fix
    directly rather than bounce back again.
  - Merged today after CI passed clean on both counts.
- **Claude, documented exception** — three more frontend fixes made
  while getting the app actually reachable on Azure (a different origin
  from the API for the first time), landed as
  [PR #49](https://github.com/nahunp/todo-app/pull/49):
  - `todo-list.ts`'s `load()`/`create()` never actually used
    `TodoListService` — hardcoded relative-path `HttpClient` calls left
    over from before the service existed. Only surfaced once frontend
    and backend were genuinely different origins; worked fine locally
    against `ng serve`'s own proxy. Rewired both to go through the
    service, added `TodoListService.createList()`.
  - Replaced a first attempt at build-time `environment.ts`/
    `environment.prod.ts` + `fileReplacements` (superseded before
    merging, in [PR #48](https://github.com/nahunp/todo-app/pull/48))
    with a runtime `window.__appConfig` config set by
    `public/config.js`, loaded via a plain `<script>` before Angular's
    bundle. See `runtime-config.ts`'s doc comment for the full story —
    what looked like an esbuild bug turned out to just be the
    `todo-list.ts` issue above.
  - Added `public/staticwebapp.config.json` (`navigationFallback`) —
    Static Web Apps 404s on a direct load of any client-side route
    (e.g. `/register`) without it.

- **[PR #58](https://github.com/nahunp/todo-app/pull/58)** — Copilot had
  started applying the Cloud Dancer brand standards across the app
  (colors, type, spacing) but ran out of tokens mid-task. Claude picked
  up the existing WIP branch instead of starting over, finished it, and
  found a real concurrency bug along the way while double-click testing
  the result (logged as backend [PR #57](https://github.com/nahunp/todo-app/pull/57)).
- **[PR #61](https://github.com/nahunp/todo-app/pull/61)**, Claude,
  documented exception (Copilot unavailable) — Diego's ask after his own
  registration mistake (reused a weak password): a live password
  requirements checklist on `/register`, driven by the backend's new
  `GET /password-policy` endpoint so it can never drift from what's
  actually enforced; the Cloudflare Turnstile widget; a "don't reuse your
  real password" warning; and Terms of Service + Privacy Policy pages
  linked from a new app footer, since Diego plans to share the live link
  publicly and wanted to be upfront this is a personal learning project,
  not a real company. Verified end-to-end locally with Cloudflare's test
  key/secret pair first, then again in production after Diego set the
  real key pair himself.
- **[PR #62](https://github.com/nahunp/todo-app/pull/62)**, Claude,
  documented exception — three small UX issues Diego found live-testing
  the deployed app: the internal list ID was rendering next to every
  list name with no value to the user; the "Lists" nav link was visible
  (and looked broken) while logged out; and Logout cleared the session
  but never navigated anywhere, so it looked like it had silently
  failed. Verified locally, not against production — Cloudflare's
  Turnstile has been crashing the automated browser tool since the real
  site key went live (suspected bot detection), so production
  verification for anything touching `/register` now has to be done by
  Diego himself, in his own browser.

## Plan for tomorrow

- No open frontend issues right now.
- Worth a look eventually: the delete-confirm button (and others) has
  no `[disabled]` guard against a rapid double-click firing the request
  twice — the backend now handles the resulting race gracefully (404,
  not 500, as of today's backend PR #51), but a UI-side guard would
  avoid the wasted second request and the momentary error state
  entirely.

## Blockers

- Copilot is out of tokens, not just missing its GitHub API token — it
  can't do any work right now. Diego asked Claude to keep covering
  `frontend/` directly until he confirms Copilot's back.
